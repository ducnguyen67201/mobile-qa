# Local example only. Config uses fixed isolated development/test database URLs.
# DATABASE_URL is only read by production configuration.
MOBILE_USE_TELEMETRY_ENABLED=false
# Production only, when deployment is implemented:
# DATABASE_URL=postgres://USER:PASSWORD@HOST/DATABASE
# HOST=https://YOUR_HOST
